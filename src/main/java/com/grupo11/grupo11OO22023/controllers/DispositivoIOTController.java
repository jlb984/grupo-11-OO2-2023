package com.grupo11.grupo11OO22023.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/dispositivoiot")
public class DispositivoIOTController {
	/*
	 * @Autowired private DispositivoIOTService dispositivoIOTService;
	 * 
	 * 
	 * @Autowired private BaniosPublicosService baniosPublicosService;
	 * 
	 * 
	 * @GetMapping public List<DispositivoIOT> getDispositivoIOT(){ return
	 * this.dispositivoIOTService.lstDispositivosIOT(); }
	 * 
	 * @GetMapping(path = "/{id}") public Optional<DispositivoIOT>
	 * getbyId(@PathVariable("id") int id ){ return
	 * this.dispositivoIOTService.getDispositivoIOTporId(id); }
	 * 
	 * 
	 * @GetMapping public List<BaniosPublicos> getBaniosPublicos(){ return
	 * this.baniosPublicosService.lstBaniosPublicos(); }
	 * 
	 * @PostMapping public BaniosPublicos saveBaniosPublicos(@RequestBody
	 * BaniosPublicos baniosPublicos) { return
	 * baniosPublicosService.saveBaniosPublicos(baniosPublicos); }
	 */
}
